{
  "name": "submarine-cable-map",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run build:map && npm run build:convert",
    "download": "curl https://www.submarinecablemap.com/api/v3/cable/cable-geo.json > data/cable-geo.json",
    "build:map": "node map.js",
    "build:chord": "node chord.js",
    "build:convert": "./convert.sh",
    "clean": "rm -f output/*"
  },
  "dependencies": {
    "d3": "^7.3.0",
    "d3-geo": "^3.0.1",
    "d3-node": "^2.2.3",
    "jsdom": "^19.0.0"
  },
  "devDependencies": {
  }
}
